<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="20dp"
    android:background="@color/white">

    <!-- Title -->
    <TextView
        android:id="@+id/tv_sensors_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Sensor Data"
        android:textSize="24sp"
        android:textStyle="bold"
        android:textColor="@android:color/black"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="40dp"/>

    <!-- Motion Sensor -->
    <TextView
        android:id="@+id/tv_motion_sensor"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Motion Sensor: Loading..."
        android:textSize="18sp"
        android:textColor="@android:color/darker_gray"
        android:padding="10dp"
        app:layout_constraintTop_toBottomOf="@id/tv_sensors_title"
        app:layout_constraintStart_toStartOf="parent"/>

    <!-- Temperature Sensor -->
    <TextView
        android:id="@+id/tv_temperature_sensor"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Temperature: Loading..."
        android:textSize="18sp"
        android:textColor="@android:color/darker_gray"
        android:padding="10dp"
        app:layout_constraintTop_toBottomOf="@id/tv_motion_sensor"
        app:layout_constraintStart_toStartOf="parent"/>

    <!-- Refresh Button -->
    <Button
        android:id="@+id/btn_refresh_sensors"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Refresh Data"
        app:layout_constraintTop_toBottomOf="@id/tv_temperature_sensor"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="30dp"/>

</androidx.constraintlayout.widget.ConstraintLayout>
